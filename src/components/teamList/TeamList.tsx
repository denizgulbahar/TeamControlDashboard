import React from 'react';

type TeamListProps = {
  // Buraya prop tanımlamaları eklenebilir
};

const TeamList: React.FC<TeamListProps> = () => {
  return (
    <div>
      <h1>Team List</h1>
    </div>
  );
};

export default TeamList;